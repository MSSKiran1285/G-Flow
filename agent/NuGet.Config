<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <clear />
    <!-- api.nuget.org is unreachable from dotnet.exe on this machine (network policy blocks
         its TLS stack; curl/urllib are unaffected — see docs/assumptions.md). Packages were
         pre-resolved and mirrored into this local folder via scripts/resolve_nuget.py. -->
    <add key="offline" value="C:\Users\703393028\.nuget-offline-feed" />
  </packageSources>
</configuration>
